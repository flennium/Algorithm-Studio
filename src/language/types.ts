export type LanguageLocale = "fr" | "en";
export type Value = number | string | boolean;
export type SourcePosition = { offset: number; line: number; column: number };
export type SourceSpan = { start: SourcePosition; end: SourcePosition };
export type Diagnostic = { severity: "error" | "warning"; code: string; message: string; span: SourceSpan; suggestion?: string };
export type RunResult = { output: string[]; diagnostics: Diagnostic[]; variables: Record<string, Value> };
export type Expr = { kind: "literal"; value: Value } | { kind: "name"; name: string } | { kind: "unary"; op: string; right: Expr } | { kind: "binary"; op: string; left: Expr; right: Expr };
export type Statement = { kind: "assign"; name: string; value: Expr; line: number } | { kind: "write"; values: Expr[]; line: number } | { kind: "if"; condition: Expr; yes: Statement[]; no: Statement[]; line: number } | { kind: "while"; condition: Expr; body: Statement[]; line: number } | { kind: "repeat"; condition: Expr; body: Statement[]; line: number } | { kind: "for"; name: string; start: Expr; end: Expr; step: Expr; body: Statement[]; line: number };
export type Program = { name: string; declarations: string[]; constants: Array<{ name: string; value: Expr; line: number }>; statements: Statement[] };
export const spanFor = (line: number): SourceSpan => ({ start: { offset: 0, line, column: 1 }, end: { offset: 0, line, column: 2 } });
export const tr = (locale: LanguageLocale, fr: string, en: string): string => locale === "en" ? en : fr;
